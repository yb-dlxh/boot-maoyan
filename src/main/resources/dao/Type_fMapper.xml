<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bootmaoyan.dao.Type_fMapper">
    <select id="selectAll" resultType="Type_f">
        select * from type_f
    </select>

    <select id="selectById" resultType="Type_f" parameterType="Type_f">
        select * from type_f where id=#{id}
    </select>
</mapper>